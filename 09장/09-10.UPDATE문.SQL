SET ECHO ON
SET TAB OFF
SET SERVEROUTPUT ON

REM 기본 UPDATE문
BEGIN
  UPDATE emp
     SET deptno = 40
   WHERE empno = 9000 ;
  DBMS_OUTPUT.PUT_LINE('UPDATE 건수: '||SQL%ROWCOUNT) ; -- 변경된 건수 출력
  COMMIT ;
END ;
/
