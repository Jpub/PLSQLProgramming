SET ECHO ON
SET TAB OFF
SET SERVEROUTPUT ON

REM SHOW ERROR는 딕셔너리 뷰 USER_ERRORS를 조회하는 것임
SELECT LINE||'/'||POSITION AS "LINE/COL"
     , TEXT                AS  ERROR 
  FROM USER_ERRORS 
 WHERE NAME = 'GET_WAGE' -- 함수명은 대문자를 사용
 ORDER BY LINE, POSITION ;
